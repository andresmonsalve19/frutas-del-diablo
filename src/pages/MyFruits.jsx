import { Container, Typography } from "@mui/material";

export const MyFruits = () => {
    return (
        <Container maxWidth="xl">
            <Typography variant="h1" textAlign="center">Mis frutas</Typography>
        </Container>
    );
};