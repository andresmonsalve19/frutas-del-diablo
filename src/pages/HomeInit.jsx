import { Container, Typography } from "@mui/material";

export const HomeInit = () => {
    return (
        <Container maxWidth="xl">
            <Typography variant="h1" textAlign="center" >Home Page Init</Typography>
        </Container>
    );
}; 