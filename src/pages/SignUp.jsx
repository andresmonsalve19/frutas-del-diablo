import { Container, Typography } from "@mui/material";

export const SignUp = () => {
    return (
        <Container maxWidth="xl">
            <Typography variant="h1" textAlign="center" >Registrarse</Typography>
        </Container>
    );
}; 
