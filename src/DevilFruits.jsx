import { BrowserRouter } from "react-router-dom";
import { Router } from "./Router";

export const DevilFruits = () => {
    return (
        <BrowserRouter>
            <Router />
        </BrowserRouter>
    );
};
